Python file that automates the checkout process using selenium 
currently working on finalizing the checkout process 
so far the program is able to select a specifc item by name and add it to cart then show the checkout window.
